name: Test Basic

on:
  push:
    branches: [ main, develop ]

jobs:
  basic-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: echo "✅ Workflow test passed"
